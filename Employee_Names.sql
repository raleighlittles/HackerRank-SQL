/*
 * Name is the column we want to query, so we pass it to SELECT, and we use the ORDER BY
 * clause to sort our results in terms of names, in ascending order.
*/

SELECT NAME FROM EMPLOYEE ORDER BY NAME ASC